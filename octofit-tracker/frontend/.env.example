# Required: the Codespaces name used to build the API base URL
# https://${VITE_CODESPACE_NAME}-8000.app.github.dev/api/
# Copy this file to .env.local and set the value. When unset, the app
# falls back to http://localhost:8000/api for local development.
VITE_CODESPACE_NAME=
